---
// src/pages/team.astro
import Layout from '../layouts/Layout.astro';

// Función para generar IDs seguros
function slugify(str: string): string {
  return str
    .toLowerCase()
    .trim()
    .replace(/[^a-z0-9áéíóúñ\s]/g, '')
    .replace(/\s+/g, '-');
}

// Datos del equipo
const team = [
  {
    name: 'Juan Esteban Pinzon',
    role: 'CEO & Fundador',
    bio: 'Apasionado por crear soluciones que marcan la diferencia.',
    photo: '/juan.png',
    link:'/team/juan-esteban-pinzon'
  },
  {
    name: 'Ronald Torres',
    role: 'Desarrollador Full Stack',
    bio: 'Fanático de Astro, React y el código limpio. Siempre buscando la solución más eficiente.',
    photo: '/riuk.jpg',
    link:'/team/ronald-torres'
  },
];
---

<Layout title="Nuestro Equipo">
  <section class="py-20">
    <div class="container mx-auto px-4 text-center max-w-5xl">
      <h1 class="text-4xl md:text-5xl font-bold text-white mb-6">Nuestro equipo</h1>
      <p class="text-xl text-white/85 mb-12">
        Personas apasionadas por el diseño, el código y las buenas ideas.
      </p>

      <!-- Grid de tarjetas -->
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-10">
        {team.map((member) => (
          <>
            <!-- Tarjeta -->
            <div class="bg-white/10 backdrop-blur-sm border border-white/20 rounded-xl overflow-hidden hover:scale-105 hover:shadow-2xl hover:shadow-purple-900/20 transition duration-300">
              <img
                src={member.photo}
                alt={member.name}
                class="w-full h-64 object-cover object-center"
              />
              <div class="p-6 text-center">
                <h3 class="text-xl font-bold text-white">{member.name}</h3>
                <p class="text-purple-300 font-medium mb-3">{member.role}</p>
                <p class="text-white/75 text-sm leading-relaxed">{member.bio}</p>
                <a href={member.link} class="mt-8 inline-block px-8 py-3 bg-white/20 backdrop-blur-sm border border-white/30 rounded-full font-semibold hover:bg-white/30 transition hover:scale-105">
		              Ver perfil »
	              </a>
              </div>
            </div>
          </>
        ))}
      </div>
    </div>
  </section>
</Layout>